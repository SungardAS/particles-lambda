{{
  parameter "m:core" "base"
  type="Number"
  minValue="128"
  maxValue="1536"
  default="128"
  description="Amount of memory, in MB, that is allocated to the Lambda function. Must be a multiple of 64 between 128 and 1536."
}}
